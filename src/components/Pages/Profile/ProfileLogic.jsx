import React from 'react';
import ProfileUI from "./ProfileUI";

const ProfileLogic = () => {
    return (
        <div>
            <ProfileUI/>
        </div>
    );
};

export default ProfileLogic;
